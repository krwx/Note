# Django

- `python manage.py collect statics`
  - 作用：收集静态文件，即生成 Django admin 等使用的 css、html、js 等文件

不懂：

- `python manage.py makemigration`
- `python manage.py migrate`
- `python manage.py check --deploy`
