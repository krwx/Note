# 注释

SQL 支持在代码中添加注释，以提高代码的可读性和维护性。注释不会被 SQL 引擎执行。

## 单行注释

使用 `--` 开始的内容为单行注释：

```sql
-- 这是一个单行注释
SELECT * FROM Products;
```

## 多行注释

使用 `/* */` 包围的内容为多行注释：

```sql
/*
这是一个多行注释
可以跨越多行
*/
SELECT * FROM Products;
```
