# 版本变化

## v7 -> v8

Vite 8 使用基于 Rolldown 和 Oxc 的工具，而不是 esbuild 和 Rollup。

- 依赖优化使用 Rolldown 而不是 esbuild
- 使用 Oxc 进行 JavaScript 转换，而不是 esbuild。
- 使用 Oxc 压缩器进行 JavaScript 压缩，而不是 esbuild。

## v2 -> v3

vite 不使用 terser，而是使用 esbuild 进行生产构建中的代码压缩，这使得构建速度更快。
